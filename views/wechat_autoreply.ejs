<%- include header %>
    <div class="jumbotron">
        <h1><%= title %></h1>
        <form method="post">
            <div class="form-group">
                <label class="col-sm-2 control-label">添加好友自动回复</label>
                <div class="col-sm-10">
                    <select class="form-control">
                            <option value="0">否</option>
                            <option value="1"
                                    <% if(addfriend){ %>
                                        selected
                                    <% }%>
                                    >是</option>
                    </select>
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-2 control-label">自动回复</label>
                <div class="col-sm-10">
                    <select class="form-control">
                        <option value="0">否</option>
                        <option vlaue="1"
                                <% if(autoreply){ %>
                                    selected
                                <% } %>
                                >是</option>
                    </select>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-11 col-sm-1">
                    <button type="submit" class="btn btn-default">提交</button>
                </div>
            </div>
        </form>
    </div>
<%- include footer %>